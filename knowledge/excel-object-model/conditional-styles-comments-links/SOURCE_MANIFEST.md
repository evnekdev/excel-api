# Prompt 16 source manifest

This evidence set is derived from the registered local Microsoft Excel type library through `tools/excel-object-model-inventory`; it records the object and member names used by the safe `excel-com` wrappers. The normalized knowledge base also retains the linked Microsoft Learn reference URLs generated by that tool.

No external artifact was downloaded for this prompt. The only runtime source is a controlled, fresh Excel Automation session owned by the test process. No raw `IDispatch` pointer, account identity, user workbook, VBA project, or external hyperlink target is captured here.

The package covers `FormatConditions`, conditional subtypes, effective `DisplayFormat`, `Styles`/`Style`, legacy `Comment` (modern Excel UI: Note), inspected read-only threaded-comment surfaces, and `Hyperlinks`.
