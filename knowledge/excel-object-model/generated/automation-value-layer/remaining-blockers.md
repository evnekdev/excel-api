# Remaining blockers

No public wrapper or stable API is implied. The remaining host-specific blocker is the controlled live compatibility run, which must follow a passing raw `Workbooks.Add` smoke control and verify natural exit for every owned process.
