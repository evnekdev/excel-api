# Internal Automation value model

`AutomationValue` distinguishes `Empty`, `Null`, Boolean, ordinary `Number(f64)`, UTF-16 text, `ExcelError`, `OaDate`, exact `Currency`, and a rectangular array. `Missing` is an invocation argument only, never a worksheet value.
