# Conversion policies

The strict default rejects non-finite numbers, embedded NUL, unsupported ranks and types, lossy integers, invalid date use, malformed shapes, and short Excel error numbers. `ShapePolicy::Exact` is the only available default behavior.
