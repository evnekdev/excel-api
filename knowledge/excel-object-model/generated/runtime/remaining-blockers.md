# Remaining Prompt 05D blockers

Generated by `excel-com-range-probe`; do not edit by hand. Classifications distinguish the client implementation from session and host state rather than assigning intermittent failures to Rust by default.

Runtime observations are version-specific and do not define a public API.

| Mode | Target | Difference classification | Detail |
| --- | --- | --- | --- |
| `comtypes-dynamic` | `Prompt 05 Range smoke through Workbooks.Add` | `inconclusive` | The bounded Workbooks.Add operation did not return an owned workbook dispatch object, so this mode did not enter the A1.Value2 smoke test. |
| `comtypes-generated` | `comtypes generated dual-interface vtable parity` | `inconclusive` | The installed typelib confirms dual candidates, but no generated Rust Excel bindings are available. The probe records a raw IDispatch fallback and does not hand-write a vtable layout. |
| `comtypes-generated` | `Prompt 05 Range smoke through Workbooks.Add` | `inconclusive` | The bounded Workbooks.Add operation did not return an owned workbook dispatch object, so this mode did not enter the A1.Value2 smoke test. |
| `pywin32-dynamic` | `Prompt 05 Range smoke through Workbooks.Add` | `inconclusive` | The bounded Workbooks.Add operation did not return an owned workbook dispatch object, so this mode did not enter the A1.Value2 smoke test. |
| `pywin32-generated` | `Prompt 05 Range smoke through Workbooks.Add` | `inconclusive` | The bounded Workbooks.Add operation did not return an owned workbook dispatch object, so this mode did not enter the A1.Value2 smoke test. |
| `rust-baseline` | `Prompt 05 Range smoke through Workbooks.Add` | `inconclusive` | The bounded Workbooks.Add operation did not return an owned workbook dispatch object, so this mode did not enter the A1.Value2 smoke test. |
