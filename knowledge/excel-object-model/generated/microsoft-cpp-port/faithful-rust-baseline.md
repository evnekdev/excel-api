# Faithful Rust baseline

The independent `windows-sys` Rust control completed 20 of 20 fresh runs with the faithful Microsoft call fields. Each wrote and read the same 15 by 15 matrix, called `Quit` exactly once, and verified natural exit. There were no failure stages or forced terminations.
