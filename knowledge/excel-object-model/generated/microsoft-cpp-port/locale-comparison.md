# Locale comparison

The faithful controls keep Microsoft's asymmetry: `LOCALE_USER_DEFAULT` (1024) for `GetIDsOfNames` and `LOCALE_SYSTEM_DEFAULT` (2048) for `Invoke`. A five-run project-LCID invocation control also completed, so locale selection was not isolated as the historical failure cause.
